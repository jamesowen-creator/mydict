# METIS-3 프로젝트 진행상황

`HANDOVER.md`(인수인계서), `PRD.md`(제품 정의 + 향후 백로그)와 함께
참고. 아래 작업 번호는 대화 세션 내 지시 단위이며 커밋 메시지 자체에는
번호가 남지 않으므로, 특히 1~8번은 코드에 남은 주석과 커밋 시각을
근거로 재구성한 것 — 정확도가 떨어지는 항목은 "확인 필요"로 표시함.

---

## 완료된 작업 목록

### 확실 (코드 주석 또는 이번 세션에서 직접 확인)

| 작업 | 내용 | 커밋 |
|------|------|------|
| 작업2 | Live OCR 스캐너 도입 — metis2-app 참고 UI/UX 재설계, 후보 랭킹 알고리즘 포팅 | `8b51f4c`, `f31e5c9` |
| 작업2-보완3 | OCR 후보를 실제 영어 단어 DB(word-list)로 검증하는 필터 추가 | `f17a023` |
| 작업3 | `server.js`(~1,025줄) → 기능별 라우터 분리 리팩터링, 미사용 파일 정리, 검색 탭 제거 | `8fae4ff`, `6d891a0`, `534b7e3`, `b521fba`, `0d65177`(추정) |
| 작업5 | 단어장을 좌측 컨트롤 레일 + 우측 목록 2단 레이아웃으로 개편 | `eb5a5b5` (관련: `f95c0cf` 액션바 sticky, `961632c` 스타일 통일) |
| 작업5-a | 좌측 컨트롤 레일이 스크롤 시 화면 밖으로 사라지는 버그 수정(sticky 처리) | `aac6cef` |
| 작업7 | OCR 실시간 화면 컨트롤 버튼 위치/아이콘/투명도 수정 (이전 지시가 미반영되어 재전달됨) | `f40b03d` |
| 작업8 | 홈 화면을 대시보드 → 3D 휠 피커로 전면 교체, 로그인/로그아웃/관리자 버튼 추가, 라이트 테마 적용 | `9950a9b`, `12eea75`, `e1524f8` |
| 작업9 | 신규 가입자 승인제 — 현황 파악 및 설계안 제시 (조사만, 코드 변경 없음) | 커밋 없음 |
| 작업9-a | 승인제 실제 구현: `is_approved` 컬럼, 가입 시 기본 false(관리자 이메일은 true), 관리자 패널 승인 UI | `2c8396d` |
| 작업9-b | OAuth 로그인이 항상 `/`로만 리다이렉트되던 회귀 수정(시작 페이지로 복귀하도록) | `44b1648` |
| 작업9-c | 특정 계정 로그인 루프 조사(임시 디버그 로그) → JWT `atob()` 디코딩이 한글 이름을 깨뜨리던 실제 버그 발견/수정, 디버그 로그 제거 | `4fe5921`~`d5ce3c4`, `7ecf14a` |
| 작업10(+10-a,10-b) | 홈 화면 3D 휠을 실기기 기준 한 화면에 들어오게 조정(휠 재중앙, 아이콘 제거, 태그라인 제거) | `e1524f8` (추정 — 이번 세션 시작 이전 완료분) |
| 작업11 | 홈 화면이 실기기(주소창 등 브라우저 UI)에서도 스크롤 없이 맞도록 `h-dvh`→`h-svh` 전환 + safe-area 처리 | `7f4324f` |
| 작업12 | 사전 페이지 상단 nav를 하단 고정으로 이동, 검색 탭 부활, 검색창을 화면 1/3 지점으로 재배치 | `6064dd4` |
| 작업12-a | 하단 nav의 safe-area 처리 확인(이미 적용돼 있어 코드 변경 없음) | 커밋 없음 |
| 작업12-b | 로고를 v4 워드마크 이미지로 교체, nav 탭 간격 균일화, 뱃지를 라벨 위로 이동 | `edef7f6` |
| 작업13 | 전 화면 공용 상단 헤더(로고+로그인상태) 추가, 검색창 위치 애니메이션 | `5c0b6f5` |
| 작업13-a | MetaCong에도 공용 헤더 추가(소스 없어 정적 `index.html` 패치 방식) | `96d93d1` |
| 작업13-b | 공용 헤더 로고를 이미지에서 코랄 "METIS" 텍스트로 변경 | `148f22f` |
| 작업5-a(재확인) | 단어장 좌측 레일 sticky 재수정(공용 헤더 실측 높이 48px 반영, `#wb-select-row`의 낡은 52px 값도 함께 수정) | `aac6cef` |
| 작업4-a | "사전적 의미"를 짧은 한국어 대응어 나열로 간결화 | `a27f94b` |
| 작업15 | 검색창 재포커스 시 기존 텍스트 전체 선택 | `34f9991` |
| 작업14 | 사전 검색을 2단계 AI 호출로 분리(빠른 1차 렌더 + 2차 상세 채움) | `82c7425` |
| 작업16 | 단어장 "선택 퀴즈" 모드 추가 | `44d4400` |
| 문서화 | PRD.md 백로그에 OCR 다국어/정확도 항목 추가 | `8b0f966` |
| 작업16-a | "다시 풀기" 시 선택 퀴즈 범위(customPool) 유지하도록 수정 | `a7d7c0b` |

### 확인 필요 (이번 세션 근거로는 확정 불가)

- **작업1**: 내용 불명. Live OCR 스캐너 최초 추가(`1d290dd`)나 Google OAuth/DB 연동 셋업(`c894dca`)일 가능성이 있으나 직접적 근거 없음.
- **작업4**: 내용 불명. `PRD.md` 3장에 "(작업 4 참고)"라는 언급이 로컬 env 제약 관련해서 남아있지만, 정확히 무엇을 지시한 작업인지는 이번 세션에서 확인 불가.
- **작업6**: 코드/커밋 어디에도 흔적을 찾지 못함.

---

## 현재 상태

- **배포:** 정상 배포 중으로 추정(마지막 push 후 별도 장애 보고 없음 — 다만 이 세션은 Railway 대시보드에 직접 접근하지 않았으므로 실제 배포 성공 여부는 Railway에서 최종 확인 필요)
- **로컬 개발 환경:** `DATABASE_URL`/`GOOGLE_CLIENT_ID` 미설정 — DB 저장/Google 로그인 기능은 로컬에서 항상 실패(500)하는 것이 정상. `ANTHROPIC_API_KEY`는 설정되어 있어 실제 사전 검색 AI 호출은 로컬에서도 동작 확인됨.
- **마지막으로 확인된 주요 기능 (이번 세션 중 Playwright로 직접 검증됨):**
  - 신규 가입 승인제(관리자 승인 전 로그인 차단) 및 관리자 패널 승인 UI
  - 홈 화면 3D 휠 피커 — 실기기 기준 뷰포트에서도 스크롤 없이 표시
  - 사전 페이지: 하단 고정 nav(검색/단어장/퀴즈/복습/오답노트, 5개 중 4개 노출+가로 스크롤), 공용 상단 헤더, 검색창 위치 애니메이션, 2단계 검색 응답, 검색창 재포커스 전체선택
  - 단어장: 좌측 컨트롤 레일 sticky, 선택 듣기/삭제 모드/선택 퀴즈 3모드 상호 배타 동작, 선택 퀴즈 "다시 풀기" 범위 유지
  - OCR 스캐너: 컨트롤 버튼 위치/아이콘/투명도
  - 공용 헤더가 문학나침반/한입독서/관리자/MetaCong까지 5개 화면에 반영됨

---

## 다음에 이어서 하면 좋을 것

1. **PPT 나머지 페이지 반영** — 이번 세션은 PPT 7~10페이지 피드백까지 처리함(작업12, 작업13, 작업16 등). 남은 페이지가 있다면 이어서 진행.
2. **`server.js` 잔여 정리** — 작업3에서 의도적으로 남겨둔 죽은 API 4개(`/api/files`, `/api/debug`, `/api/auth`, `/api/search`) 정리 여부 결정. 특히 `/api/debug`는 환경변수 존재 여부/길이를 노출하므로 보안 관점에서 우선순위 있음(`PRD.md` §7에도 이미 언급됨).
3. **MetaCong 소스 접근 경로 확보 검토** — 현재 정적 파일 패치(공용 헤더)로 우회 중이나, 외부 프로젝트가 재빌드/재동기화되면 조용히 사라지는 구조적 리스크가 있음. 소스 저장소를 이 저장소에 통합하거나(서브모듈/모노레포), 최소한 "동기화 시 재적용해야 할 patch 목록" 문서화가 필요.
4. **`HANDOVER.md`의 "확인 필요" 항목 해소** — 작업1/4/6의 정확한 내용을 파악할 수 있는 이전 세션 로그가 있다면 채워 넣을 것.
5. **원인 미확정 로그인 루프 재발 여부 관찰** — `HANDOVER.md` §4 참고. 재발 시 클라이언트 측(특히 실기기 브라우저 콘솔) 로그 확보가 관건.
